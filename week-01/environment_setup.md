# Environment Setup

This week I configured the local Python environment using Conda and installed the necessary packages for data extraction:

```bash
conda create -n ml_env python=3.11
conda activate ml_env
pip install pandas numpy jupyter
```
